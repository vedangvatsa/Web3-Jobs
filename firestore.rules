rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public write access to the subscribers collection for email capture
    match /subscribers/{subscriberId} {
      allow write: if true; // Changed from 'create' to 'write' for broader permission
      allow read, update, delete: if false;
    }
  }
}
